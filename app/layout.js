export const metadata = {
  title: "Stock System",
  description: "Simple stock system",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>
        {children}
      </body>
    </html>
  );
}
